# Copy to .env and fill in. The auto-tester uses Google Gemini.
GEMINI_API_KEY=
# Optional model overrides (defaults shown):
AUTO_TESTER_MODEL_PRO=gemini-2.5-pro
AUTO_TESTER_MODEL_FLASH=gemini-2.5-flash
